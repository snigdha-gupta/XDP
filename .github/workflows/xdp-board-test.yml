name: XDP Board Test

# Disabled for now. Re-enable after vek385 B-revB2 board farm access is confirmed.
# Scripts live under ci/board/ve2/ for future zboard + xrt-smi tests.

on:
  workflow_call:
    inputs:
      build_run_id:
        required: true
        type: string
  workflow_dispatch:
    inputs:
      build_run_id:
        description: XDP Build run ID with VE2 RPM artifacts
        required: true
        type: string

jobs:
  board-test-ve2:
    if: false
    runs-on: [self-hosted, Ubuntu-22.04]
    steps:
      - run: echo "VE2 board test disabled"

  board-test-edge:
    if: false
    runs-on: [self-hosted, Ubuntu-22.04]
    steps:
      - run: echo "Edge board test disabled"
